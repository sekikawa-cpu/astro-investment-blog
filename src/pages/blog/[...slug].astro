---
import { getCollection } from 'astro:content';
export async function getStaticPaths() {
  const blogEntries = await getCollection('blog');
  return blogEntries.map(entry => ({ params: { slug: entry.slug }, props: { entry } }));
}
const { entry } = Astro.props;
const { Content } = await entry.render();
---
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <title>{entry.data.title}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body class="bg-slate-50 text-slate-800 font-sans min-h-screen flex flex-col">
    <header class="bg-slate-900 text-white shadow-md py-4">
      <div class="max-w-5xl mx-auto px-4 flex justify-between items-center">
        <a href="/" class="text-xl md:text-2xl font-bold tracking-wider hover:text-blue-300 transition-colors">配当＆優待ナビ 💸</a>
        <a href="/" class="text-sm text-slate-300 hover:underline">一覧に戻る</a>
      </div>
    </header>

    <main class="flex-grow max-w-5xl mx-auto px-4 py-10 w-full bg-white shadow-sm my-6 rounded-lg">
      <article class="prose prose-slate max-w-none px-6 md:px-10">
        <header class="mb-10 border-b border-slate-100 pb-8">
          <p class="text-blue-600 font-bold mb-3">{new Date(entry.data.pubDate).toLocaleDateString('ja-JP', { year: 'numeric', month: 'long', day: 'numeric' })}</p>
          <h1 class="text-2xl md:text-3xl font-extrabold text-slate-900 leading-tight">{entry.data.title}</h1>
        </header>
        <div class="markdown-content"><Content /></div>
      </article>
    </main>

    <footer class="bg-slate-900 text-slate-400 text-center py-10 mt-auto">
      <p class="text-sm">&copy; 2026 配当＆優待ナビ. All rights reserved.</p>
    </footer>
  </body>
</html>

<style is:global>
  /* 導入文スタイル */
  .lead-text { @apply text-lg text-slate-600 leading-relaxed mb-12 p-6 bg-blue-50 rounded-xl border-l-4 border-blue-500 italic; }

  /* 共通 */
  .markdown-content h2 { @apply text-2xl font-bold text-slate-800 mt-16 mb-8 border-l-8 border-blue-600 pl-4 bg-slate-50 py-2; }
  .markdown-content h3 { @apply text-xl font-bold text-blue-700 mt-12 mb-4 border-b-2 border-blue-100 pb-2; }

  /* テーブル */
  .table-wrapper { @apply overflow-x-auto my-10 shadow-sm rounded-lg border border-slate-200; }
  table { @apply w-full border-collapse bg-white text-sm md:text-base; }
  th { @apply bg-slate-800 text-white p-4 font-bold text-center; }
  td { @apply p-4 border-b border-slate-100; }
  tr:nth-child(even) { background-color: #f8fafc; }

  /* 色分けルール */
  tr.row-up td { color: #ef4444 !important; }   /* プラスは赤 */
  tr.row-down td { color: #22c55e !important; } /* マイナスは緑 */
  tr.row-up td strong, tr.row-down td strong { color: inherit; }

  /* 書籍リスト */
  .book-item { @apply flex flex-col md:flex-row items-center gap-6 mb-10 border border-slate-100 p-6 rounded-xl bg-slate-50; }
  .book-item img { width: 90px; height: auto; object-fit: contain; @apply rounded shadow-md flex-shrink-0 bg-white border border-slate-200; }
  .book-item .book-info { @apply flex-1 min-w-0; }
  .book-item strong { @apply text-lg text-slate-900 block mb-2; }
  .book-item p { @apply text-sm text-slate-600 m-0 leading-relaxed; }

  .disclaimer { @apply mt-20 pt-8 border-t border-dashed border-slate-300 text-[11px] text-slate-400 italic; }
</style>
